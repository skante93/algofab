


var router = require('express').Router();


router.get('/', (req, res)=>{});

router.post('/', (req, res)=>{});


router.get('/:uid', (req, res)=>{});

router.put('/:uid', (req, res)=>{});

module.exports = router;