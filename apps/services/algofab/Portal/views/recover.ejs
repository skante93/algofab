
<!DOCTYPE html>
<html>
  <head>
    <% include partials/head %>
    
  </head>
  <body>
    <% include partials/header %>
    
    <div class="container">
    	<form method="post" class="col-sm-offset-3 col-sm-6">
    		<h1>Recover your <%= recover %></h1>
    		<% if(typeof err !== 'undefined') { %>
    			<% if( err != 'success') { %>
	    		    <div class="alert alert-danger" role="alert"> <%= err %> </div>
	                <hr />
                <% } else {%>
	                 <% if(step == 1) { %>
	            		<div class="alert alert-success" role="alert"> Successfully done, check you emails for furtehr details. </div>
	                	<hr />
	                <% } else { %>
	                	<div class="alert alert-success" role="alert"> Password updated, sign in<a href="/signin"> here.</a> </div>
	                	<hr />
	                <% } %>
                <% } %>
    		<% } %>
			

			<input type="hidden" name="recover" value="<%= recover %>">

			<% if(recover == "password"){ %>
				<% if(step == 1) { %>
					<div class="form-group row">
						<label for="username" class="col-sm-2 col-form-label col-form-label-lg">Username</label>
							<div class="col-sm-10">
								<input type="text" class="form-control form-control-lg" name="username" id="username" required/>
							</div>
					</div>
					<br/>
				<% } else { %>
					<div class="form-group row">
						<label for="pass" class="col-sm-2 col-form-label col-form-label-lg">Password</label>
							<div class="col-sm-10">
								<input type="password" class="form-control form-control-lg" name="password" id="pass" required>
							</div>
					</div>
				<% } %>
			<% } else { %>
				<div class="form-group row">
					<label for="email" class="col-sm-2 col-form-label col-form-label-lg">Email</label>
						<div class="col-sm-10">
							<input type="text" class="form-control form-control-lg" name="email" id="email" required/>
						</div>
				</div>
				<br/>
			<% } %>
			
			<div class="form-group row" >
				<button type="submit" class="btn btn-primary pull-right"> Submit </button>
			</div>

			<% if(recover == "password"){ %> 
				<div class="form-group row" >
					<div class="pull-right">
						<a href="/recover?r=username" style="margin-right: 1.5em">Forgot Username?</a>
					</div>
				</div>
			<% } else { %>
				<div class="form-group row" >
					<div class="pull-right">
						<a href="/recover?r=password" style="margin-right: 1.5em">Forgot Password?</a>
					</div>
				</div>
			<% } %>
    		<!-- <% if(typeof addemail !== 'undefined') { %>
	    		<div class="form-group row">
			    	<label for="email" class="col-sm-2 col-form-label col-form-label-lg">Email</label>
			    		<div class="col-sm-10">
			    			<input type="email" class="form-control form-control-lg" name="email" id="email" required/>
			    		</div>
			    </div>
		    <% } %>
		    
    		<div class="form-group row">
		    	<label for="username" class="col-sm-2 col-form-label col-form-label-lg">Username</label>
		    		<div class="col-sm-10">
		    			<input type="text" class="form-control form-control-lg" name="username" id="username" required/>
		    		</div>
		    </div>

		    <% if(step == 2) { %>
            <div class="form-group row">
		    	<label for="pass" class="col-sm-2 col-form-label col-form-label-lg">Password</label>
		    		<div class="col-sm-10">
		    			<input type="password" class="form-control form-control-lg" name="password" id="pass" required>
		    		</div>
		    </div>
		    <% } %>
		    <div class="form-group row" >
                    <button type="submit" class="btn btn-primary pull-right"> Submit </button>
            </div>

            <% if (step == 1) { %>
                <br/>
                <div class="form-group row" >
                    <div class="pull-right">
                        <a href="#" style="margin-right: 1.5em">Forgot Username?</a>
                    </div>
                </div>
            <% } %> -->
    	</form>
    </div>
    
    <% include partials/footer.ejs %>
  </body>
</html>
